name: Update Submodule and Run Tests

on:
  schedule:
    - cron: '0 12 * * 4'  # Runs weekly on Thursday at midday
  workflow_dispatch:

env:
  PY_COLORS: 1
  PYTEST_ADDOPTS: "--numprocesses=logical"

permissions:
  contents: write
  pull-requests: write

jobs:
  pytest:
    strategy:
      matrix:
        python-version: ["3.12"]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: "true"
          cache-suffix: pytest-${{ matrix.python-version }}
          cache-dependency-glob: "pyproject.toml"
      - name: update-submodule
        run: |
          git submodule update --init --recursive
          git submodule update --remote narwhals
      - name: install-reqs
        run: uv sync --group tests
      - name: show-deps
        run: uv pip freeze
      - name: update-tests
        run: uv run --group tests python update_run_tests.py
      - name: create-pull-request
        uses: peter-evans/create-pull-request@v8
        with:
          commit-message: 'chore: auto-update tests and submodule'
          title: 'chore: auto-update tests and submodule'
          branch: chore/test-and-submodule-auto-updates
          delete-branch: true
